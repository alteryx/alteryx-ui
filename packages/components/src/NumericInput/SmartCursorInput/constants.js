import {
  KEY_A,
  KEY_ALT,
  KEY_C,
  KEY_HOME,
  KEY_END,
  KEY_CAPS_LOCK,
  KEY_CONTEXT_MENU,
  KEY_CONTROL,
  KEY_DOWN,
  KEY_INSERT,
  KEY_LEFT,
  KEY_LEFT_CMD,
  KEY_RIGHT,
  KEY_SHIFT,
  KEY_UP,
  KEY_Y,
  KEY_V,
  KEY_X,
  KEY_Z
} from 'keycode-js';

export const CLIPBOARD_KEYS = [KEY_A, KEY_C, KEY_V, KEY_Y, KEY_X, KEY_Z];
export const UNDO_KEYS = [KEY_Z];

export const META_KEYS = [
  KEY_ALT,
  KEY_END,
  KEY_HOME,
  KEY_CAPS_LOCK,
  KEY_CONTEXT_MENU,
  KEY_CONTROL,
  KEY_INSERT,
  KEY_SHIFT,
  KEY_DOWN,
  KEY_LEFT,
  KEY_LEFT_CMD,
  KEY_RIGHT,
  KEY_SHIFT,
  KEY_UP
];
