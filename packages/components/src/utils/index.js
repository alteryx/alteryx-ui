import dataUICMaker from './dataUICMaker';

// TODO: See if we can remove this without breaking consumers
export default dataUICMaker;
export { default as dataUICMaker } from './dataUICMaker';
export { default as filterProps } from './filterProps';
